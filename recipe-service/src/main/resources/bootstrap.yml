spring:
  application:
    name: recipe-service
  data:
    mongodb:
      host: localhost
      port: 27017
      database: recipes
      username: rootuser
      password: rootpass
  cloud:
    config:
      uri: http://localhost:10100
logging:
  level:
    org:
      springframework:
        data:
          mongodb:
            core:
              MongoTemplate: DEBUG
