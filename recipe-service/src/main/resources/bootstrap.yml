spring:
  application:
    name: recipe-service
  cloud:
    config:
      uri: http://localhost:10100
eureka:
  client:
    service-url:
      defaultZone: http://localhost:10050/eureka/
logging:
  level:
    org:
      springframework:
        data:
          mongodb:
            core:
              MongoTemplate: DEBUG
