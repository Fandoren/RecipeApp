spring:
  application:
    name: uaa-service
  data:
    mongodb:
      host: localhost
      port: 27017
      database: users
      username: adminUser
      password: adminPass
  cloud:
    config:
      uri: http://localhost:10100
