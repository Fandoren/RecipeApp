spring:
  application:
    name: uaa-service
  data:
    mongodb:
      host: localhost
      port: 27017
      database: users
      username: rootuser
      password: rootpass
  cloud:
    config:
      uri: http://localhost:10100
